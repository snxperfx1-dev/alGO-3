---
inclusion: always
---

# OMEGA-F72 ARCHITECTURE LAW (always enforced)

The target system is **HyperOmega / OMEGA-F72**. It is NOT "F60", NOT "Senseei", NOT a single
decision engine. It is an evolved, layered, emergent intelligence. Obey these laws.

## L0 — ANCESTRY PRESERVATION
The system remembers every lesson it ever learned. Lineage: **Lytra → F72 → Omega → F60 → HyperOmega**.
F60 is the most advanced *substrate*, but ERF/FRZ/RIE/MCE/NE/TQE/TE/IE2/WR/DWR and Omega's risk
tools are NOT obsolete — they are observers. Never delete an engine because a newer one looks
superior. **Upgrade it: replace proxy math with authentic F60 inputs.** Amnesia is a failure mode.

## L1 — F60 IS THE SUBSTRATE (spine), NOT THE ALGORITHM
F60 OWNS: f_phys, f_se, Engine 1A phase machine, recursive curve tree, FCE/curve framework,
Invisible Network, fractal stack, chain vitality, compression persistence, participant engine,
campaign ownership, Time Intelligence. F60 does NOT own: trade qualification, target construction,
invalidation pricing, narrative, rotation, residual energy, supply/demand distance, canonical
decision output. Those are V72 observers.

## L2 — UPWARD-ONLY FLOW (closed loop)
Price → Physics → Structure → Engine1A → ERF → FRZ → Invisible Network → Curve Tree → MCE → TIE →
NE → TQE → TE → IE2 → DOE → HyperIntelligence → Risk → Execution → Position Intelligence → (loop back).
No layer may bypass, recompute, or delete a lower layer.

## L4 — F60 ENRICHMENT LAW (same observer, better inputs)
Every observer must consume authentic F60 outputs instead of old proxies:
- ERF residual ← FCE residual + compression persistence + chain vitality + curve maturity + FU continuity
- FRZ ← Invisible Network nodes + curve ownership + recursive lineage + attractor geometry
- RIE ← ownership transfer + participant interference + chain decay + compression exhaustion
- MCE ← fractal stack + curve map + network pressure + chain vitality + time intelligence
- TQE ← ERF/FCE confidence + FRZ convergence + curve persistence + chain vitality + network pressure
- TE ← network path nodes + FCE trajectory + curve ownership + attractor convergence
- IE2 ← recursive origin + parent curve + campaign ownership + compression state

## L9 — TOOL UTILIZATION (arsenal, not monarchy)
Never ask "which engine wins?" Ask "WHEN does this tool matter?" F60 engines are Tier-1 operators
(always running); ERF/FRZ/RIE/MCE/TQE/TE/IE2/Risk are called when context activates them.
The algo takes **different entry types in different contexts** (compression / rotation / network /
continuation / exhaustion / flip-zone / liquidation / expansion), possibly on different timeframes
at the same time. It thinks in **opportunities**, not signals.

## L10 — CONTINUOUS AWARENESS (campaign manager, not signal generator)
Perception + synthesis run every tick per symbol, in and out of positions. The trade is a side
effect of understanding. Post-entry **Position Intelligence** is mandatory (is the curve progressing?
chain vitality? target moved? residual collapsing? ownership transferring?). Multi-symbol from day one.

## HYPERINTELLIGENCE = COMMANDER, NOT DICTATOR
HyperIntelligence (not Senseei) is the synthesis layer. It dynamically weights every observer by
context (raise F60 trust on high compression persistence; ERF on high residual; RIE on high rotation;
FRZ near zones; MCE on HTF conflict), lets TQE veto poor opportunities, lets Risk override everything.
F60 weight is never zero; observer weights may fall toward zero but observers are never deleted.

## SENSEEI IS EXPENDABLE
Senseei is a read-only cockpit/interpreter. Nothing depends on it. Deleting it must leave the
intelligence intact. The canonical decision path is DOE → HyperIntelligence → Risk → Execution.

## RISK IS OMEGA'S INHERITANCE (permanent)
Trinity / capital / drawdown controls / kill switches / exposure limits / conviction sizing /
trade management / execution shell stay in the decision-to-order path. Deleting a risk control is
the same amnesia error as deleting an observer.

## FORBIDDEN OUTPUTS
A pure F60 file; a standalone Senseei file; a stripped "core algo"; collapsing the stack into one
engine; deleting any observer or risk control that has a downstream consumer. Preserve and enrich.
